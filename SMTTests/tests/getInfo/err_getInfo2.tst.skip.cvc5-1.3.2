cvc5's :all-statistics response embeds non-deterministic timing values (e.g. "1026ms") scattered throughout the structure, not just a single :memory line, so no stable exact-match golden is possible; see the split err_getInfo2_reasonUnknown.tst / err_getInfo2_allStatistics.tst, and scripts/getInfo-allStatistics-cvc5.scr for a supportedness/well-formedness check
