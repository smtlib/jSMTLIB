cvc5's :all-statistics response embeds non-deterministic timing values (e.g. "1026ms") scattered throughout the structure, not just a single :memory line, so no stable exact-match golden is possible; supportedness and well-formedness are instead checked by scripts/solverBehavior/all-statistics.scr
