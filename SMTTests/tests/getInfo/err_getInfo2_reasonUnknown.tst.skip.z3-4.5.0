z3-4.5.0 hangs (times out, does not respond at all) on (get-info
:reason-unknown), confirmed reproducible via direct invocation (consistent
EXIT 124/timeout, not an intermittent race) -- this build doesn't error
cleanly on the unrecognized get-info key, it just stops responding. No golden
captures a hang meaningfully; skip instead.
