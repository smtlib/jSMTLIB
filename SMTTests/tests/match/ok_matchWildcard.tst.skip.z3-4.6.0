z3-4.6.0 hangs (times out, does not respond at all) on (match ...), confirmed
reproducible via direct invocation (consistent timeout, not an intermittent
race) -- this build predates z3's SMT-LIB2 match-expression support and
apparently doesn't error cleanly on the unrecognized syntax, it just stops
responding. No golden captures a hang meaningfully; skip instead.
