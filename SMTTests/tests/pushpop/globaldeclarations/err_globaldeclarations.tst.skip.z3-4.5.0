z3-4.5.0 hangs (times out, does not respond at all) on (reset-assertions),
confirmed reproducible via direct invocation (consistent timeout, not an
intermittent race) -- same underlying issue as the other z3-4.5.0
reset-assertions skips in this directory: this build predates z3's
SMT-LIB2.5 reset-assertions support and doesn't error cleanly on the
unrecognized command, it just stops responding. No golden captures a hang
meaningfully; skip instead.
