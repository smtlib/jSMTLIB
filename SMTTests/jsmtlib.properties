# Local solver path overrides for running SMTTests.
# The application looks for jsmtlib.properties in the working directory first
# and then anywhere on the classpath (SMT/jsmtlib.properties via -cp SMT/).
# Add solver paths here to override the defaults in SMT/jsmtlib.properties.
# In CI this file is overwritten by the jsmtlib-test action with CI-specific paths.
org.smtlib.solver_simplify.exec=
